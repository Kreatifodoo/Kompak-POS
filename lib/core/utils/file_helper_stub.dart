import 'dart:typed_data';

/// Read file bytes — web stub (always returns null)
Future<Uint8List?> readFileBytes(String path) async {
  return null;
}

/// Check if file exists — web stub (always returns false)
bool fileExistsSync(String path) {
  return false;
}

/// Copy file — web stub (always returns null)
Future<String?> copyFile(String sourcePath, String destPath) async {
  return null;
}

/// Create directory — web stub (always returns false)
Future<bool> createDirectory(String path) async {
  return false;
}

/// Get file image provider — web stub (returns null)
dynamic getFileImageProvider(String path) {
  return null;
}
